import { createTheme } from "@mantine/core";

const theme = createTheme({
  other: {
    borderColor: {
      light: "1px solid #DEE2E6",
      medium: "1px solid darkgray",
      darken: "1px solid black",
    },
  },
});

export default theme;
